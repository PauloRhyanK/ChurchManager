# Dev:  cp .env.docker.example .env.docker  →  docker compose --env-file .env.docker up  (opcional)
# Prod: cp .env.production.example .env.production  →  docker compose --env-file .env.production -f docker-compose.prod.yml up -d
